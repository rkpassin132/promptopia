export const metadata = {
  title: "Create Prompts",
  discription: "Create your Ai Prompts",
};

const CreatePromptLayout = ({ children }) => {
  return <>{children}</>;
};

export default CreatePromptLayout;
