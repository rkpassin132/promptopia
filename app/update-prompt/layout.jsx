export const metadata = {
  title: "Promptopia | Update prompt",
  discription: "Discover & share ai Prompts",
};

const UpdatePromoptLayout = ({ children }) => {
  return <>{ children }</>;
};

export default UpdatePromoptLayout;